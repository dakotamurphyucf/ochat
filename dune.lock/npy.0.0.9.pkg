(version 0.0.9)

(build
 (all_platforms ((action (run dune build -p %{pkg-self:name} -j %{jobs})))))

(depends
 (all_platforms
  (camlzip dune ocaml)))

(source
 (fetch
  (url https://github.com/LaurentMazare/npy-ocaml/archive/0.0.9.tar.gz)
  (checksum md5=8a06935cb77ad546cd40872fcf7fd1e1)))
